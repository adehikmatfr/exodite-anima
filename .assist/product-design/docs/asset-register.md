# Asset register

Every font, icon set, image, and kit used in designs or shipped in the app, with its licence on record (`design-file-hygiene-and-versioning`). Status: 2026-09-20. Licence texts must be re-checked at the version actually bundled.

| Asset | Source | Licence | Terms | Owner | Used in |
|-------|--------|---------|-------|-------|---------|
| Inter (UI font) | Bundled with the app as `app/assets/fonts/Inter.ttf` (variable font from the Google Fonts repository, `ofl/inter`, 2026-09-20); licence text `Inter-OFL.txt` next to it | SIL Open Font License 1.1 | May be bundled, redistributed, and modified; the licence text must ship with the app | product-design | All screens |
| Lora (journal font) | Bundled with the app as `app/assets/fonts/Lora.ttf` (variable font, Google Fonts repository `ofl/lora`, 2026-09-20); licence text `Lora-OFL.txt` next to it | SIL Open Font License 1.1 | Same as above; the reserved font name must not be used for a modified version | product-design | Entry text (S6, S7, S8) |
| Lucide icons (icon font) | Bundled as `app/assets/fonts/Lucide.ttf` from the official `lucide-static` 1.47.0 npm package (published by the Lucide project's GitHub Actions), with its own code point list; licence text `Lucide-LICENSE.txt` next to it. 16 icons are used, listed in `app/lib/theme/app_icons.dart` (5 added 2026-09-23 for FEAT-010's mood scale: `laugh`, `smile`, `meh`, `frown`, `angry`; 3 more added 2026-09-23 for FEAT-011's photos: `camera`, `image`, `trash` (`trash-2`); every code point verified against `lucide-static@1.47.0`'s own `font/info.json`, downloaded and read directly, never guessed or taken from a summarised fetch after one such summary once gave wrong values). Not the third-party Flutter wrapper packages, to avoid an extra dependency from an individual publisher | ISC (Copyright Lucide Icons and Contributors) | May be used, copied, modified and distributed for any purpose, provided the copyright and permission notice is kept (the licence file ships with the app) | product-design, frontend-mobile | Back, add, search, clear, alert, chevron, radio choice (S2 to S13); mood scale (S7, FEAT-010); camera/library/remove (S7, S15, FEAT-011) |
| Logo and app icon | **Logo A2** (chosen by the owner 2026-09-20): a ring with a core, in the brand green. Round 1 explored four ideas (`design/logo/round1-options.png`), round 2 refined the chosen one (`design/logo/round2-options.png`). Geometry in tile units: ring centre radius 0.315, half stroke 0.046, core radius 0.088. Files: `design/logo/exodite-anima-mark.svg` (mark alone), `design/logo/exodite-anima-icon.svg` (icon), `design/logo/app-icon-1024.png`. Colours `color.sage.600` and `color.warm.50`. Built into the app by `app/tool/make_icons.py` (Android legacy, adaptive and themed icons, all iOS sizes) and `LogoMark` in `app/lib/widgets/common.dart` | project-owned (drawn by the assistant with the owner's choices) | Original artwork made for this project, covered by the repository licence; no third-party asset. No trademark search was done for the mark or the name | product-design | App icon; S1, S3, S5, S11, S12, S14 |
| Images and photos | None | not applicable | No photographs are used | none | none |

Rules: no image copied from the web or from real people; no real journal content or real passcodes in any file or preview; a new asset is added to this register before it is used.
