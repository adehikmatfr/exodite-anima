# Screen design specs (version 1)

Generated by `tools/gen_docs.py` from the screen table in that script and the acceptance criteria in the FEAT specs. Design files: `design/features/<area>/<area>-light.pen` and `-dark.pen`, exported previews in `design/exports/`. Tokens: `design-tokens.md`. Components: `component-specs.md`. Flows: `../../ux-design/report/user-flows.md`.

Status: draft, owner product-design, tokens 0.2.0. Every screen is drawn in every state listed. Screens are 390 x 844 (compact phone); tablets and landscape are not designed for version 1. Copy is draft and all strings must be externalised for translation. Numbers, dates, and counts on screen are illustrative.

Common to every screen: safe areas respected; primary action reachable by one thumb; all touch targets at least 48; error text always says what happened and what to do next; layout stays stable while loading; motion at most 300 ms and off under "reduce motion". Screens have no analytics events: the app collects none.

## S1 Welcome

| Field | Value |
|-------|-------|
| Screen | S1 Welcome |
| Feature | FEAT-004 |
| Status | draft |
| Design file | `design/features/setup-and-lock/setup-and-lock-light.pen` and `-dark.pen` |
| Flow context | F1 in `ux-design/report/user-flows.md` |

**Goal.** Understand what the app is and start. Primary action: Get started. Secondary: Import your journal (restore path, order is a proposal).

**States drawn.** Default; large text 200%.

**Reading and focus order.** Title (heading), body, the three points (list), Get started, Import your journal. The logo is decorative and hidden from screen readers.

**Accessible names and announcements.** Logo: none (decorative). Buttons use their visible text.

**Edge cases.** At 200% the content scrolls and the primary action is below the fold (finding N4). Translations up to 40% longer wrap. The three points never truncate.

**Open questions.** none: restore after passcode setup was decided on 2026-09-20.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | First launch; No passcode has been set; The timeline cannot be reached | FEAT-004 AC-1 | TC-032 |
| AC-2 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-3 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-4 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S2 Create passcode

| Field | Value |
|-------|-------|
| Screen | S2 Create passcode |
| Feature | FEAT-004 |
| Status | draft |
| Design file | `design/features/setup-and-lock/setup-and-lock-light.pen` and `-dark.pen` |
| Flow context | F1 in `ux-design/report/user-flows.md` |

**Goal.** Choose a passcode the user can remember. Primary action: Continue.

**States drawn.** Empty; valid; passcode shown; mismatch error; too easy error.

**Reading and focus order.** Back, title (heading), body, Passcode label and field, Show or Hide, Repeat label and field, helper, Continue.

**Accessible names and announcements.** Back: "Back". Show and Hide: "Show passcode" and "Hide passcode". Fields are marked secure; errors are announced when they appear; paste is allowed so password managers work; copy out of the field is not.

**Edge cases.** Continue is disabled until both fields are valid, with a sentence saying what is needed. Values the user typed are kept when an error appears. Validate on blur, then on change after the first error. The rule (at least 8 characters, not a very common passcode) is stated in the helper text and in the error.

**Open questions.** none: rules decided (`lock-and-passcode-policy`).

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The passcode step; The confirmation does not match; An error is shown and setup does not continue | FEAT-004 AC-2 | TC-033 |
| AC-2 | The passcode step; The passcode does not meet the passcode rules; An error explains the rule and setup does not continue | FEAT-004 AC-3 | TC-034 |
| AC-3 | The passcode step; A passcode of fewer than 8 characters, or one on the list of very common passcodes, is entered; The passcode is refused with a reason and setup does not continue | FEAT-004 AC-7 | TC-038 |
| AC-4 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-5 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-6 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S3 Biometrics

| Field | Value |
|-------|-------|
| Screen | S3 Biometrics |
| Feature | FEAT-004, FEAT-003 |
| Status | draft |
| Design file | `design/features/setup-and-lock/setup-and-lock-light.pen` and `-dark.pen` |
| Flow context | F1 in `ux-design/report/user-flows.md` |

**Goal.** Offer a face or fingerprint shortcut. Primary action: Turn on. Secondary: Not now.

**States drawn.** Available; not available.

**Reading and focus order.** Back, title (heading), body, Turn on, Not now (or Continue when not available).

**Accessible names and announcements.** Buttons use their visible text.

**Edge cases.** If nothing is set up on the phone, the screen says so and offers only Continue. The system prompt itself is drawn by the operating system, not by the app.

**Open questions.** none

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The lock screen; Biometrics are cancelled or fail; The passcode field is offered | FEAT-003 AC-4 | TC-024 |
| AC-2 | Another fingerprint or face was added on the device; The user unlocks with the passcode; The app opens and all entries are intact | FEAT-003 AC-8 | TC-028 |
| AC-3 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-4 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-5 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S4 No-recovery warning

| Field | Value |
|-------|-------|
| Screen | S4 No-recovery warning |
| Feature | FEAT-004 |
| Status | draft |
| Design file | `design/features/setup-and-lock/setup-and-lock-light.pen` and `-dark.pen` |
| Flow context | F1 in `ux-design/report/user-flows.md` |

**Goal.** Make sure the user knows a forgotten passcode cannot be recovered. Primary action: Start journaling, enabled only after the box is ticked.

**States drawn.** Not acknowledged; acknowledged.

**Reading and focus order.** Back, title (heading), warning card ("Important" label, two sentences), acknowledgment checkbox row, helper, Start journaling.

**Accessible names and announcements.** Checkbox: the whole row is the control (56 high), named by its sentence. The card is a group named "Important".

**Edge cases.** The disabled button is explained by the sentence "Tick the box to continue.". Warning meaning is carried by the word "Important" and the text, not by colour.

**Open questions.** none

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The warning step; The user has not acknowledged it; Setup cannot be completed | FEAT-004 AC-4 | TC-035 |
| AC-2 | Setup is completed; The app is closed and opened again; The lock screen appears and the passcode opens the empty journal | FEAT-004 AC-5 | TC-036 |
| AC-3 | The onboarding text; It is reviewed against the privacy statements; Every claim traces to a requirement and a test | FEAT-004 AC-6 | TC-037 |
| AC-4 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-5 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-6 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S5 Lock screen

| Field | Value |
|-------|-------|
| Screen | S5 Lock screen |
| Feature | FEAT-003 |
| Status | draft |
| Design file | `design/features/setup-and-lock/setup-and-lock-light.pen` and `-dark.pen` |
| Flow context | F2 in `ux-design/report/user-flows.md` |

**Goal.** Unlock the journal. Primary action: Unlock. Secondary: Use face or fingerprint.

**States drawn.** Default; wrong passcode; waiting after wrong tries; app-switcher preview; large text 200%.

**Reading and focus order.** Title, Passcode label and field, Unlock, Use face or fingerprint.

**Accessible names and announcements.** The countdown is announced once when the wait starts and again when it ends, not every second. The wrong-passcode message is announced when it appears.

**Edge cases.** No journal content is shown in any lock state, including the app-switcher preview (blank and branded). After the fifth wrong passcode the first wait is 30 seconds and doubles up to one hour; the countdown is the screen's only change. On Android screenshots are blocked.

**Open questions.** none: wait, timeout, and screenshot rules decided (`lock-and-passcode-policy`).

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The app was closed; It is opened; The lock screen appears with no entries visible | FEAT-003 AC-1 | TC-021 |
| AC-2 | The app is unlocked; It stays in the background longer than the timeout; It is locked when the user returns | FEAT-003 AC-2 | TC-022 |
| AC-3 | The app is unlocked; The user opens the app switcher; No entry content is visible in the preview | FEAT-003 AC-3 | TC-023 |
| AC-4 | The lock screen; Biometrics are cancelled or fail; The passcode field is offered | FEAT-003 AC-4 | TC-024 |
| AC-5 | The lock screen; The correct passcode is entered; The app unlocks | FEAT-003 AC-5 | TC-025 |
| AC-6 | The lock screen; Several wrong passcodes are entered; The wait before the next attempt grows | FEAT-003 AC-6 | TC-026 |
| AC-7 | Several wrong passcodes were entered; The wait is running; No journal content is visible | FEAT-003 AC-7 | TC-027 |
| AC-8 | Five wrong passcodes were entered; Another wrong passcode is entered; The app waits 30 seconds before the next attempt, and the wait doubles with each further wrong attempt up to one hour | FEAT-003 AC-9 | TC-093 |
| AC-9 | A wait is running after wrong passcodes; The app is closed and opened again; The wait continues and the count of wrong attempts is kept | FEAT-003 AC-10 | TC-029 |
| AC-10 | The app is open on Android; The user takes a screenshot or records the screen; The capture is blocked | FEAT-003 AC-11 | TC-094 |
| AC-11 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-12 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-13 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S6 Timeline

| Field | Value |
|-------|-------|
| Screen | S6 Timeline |
| Feature | FEAT-002, FEAT-008 |
| Status | draft |
| Design file | `design/features/journal/journal-light.pen` and `-dark.pen` |
| Flow context | F3, F7 in `ux-design/report/user-flows.md` |

**Goal.** Browse entries and start a new one. Primary action: New entry.

**States drawn.** Entries; with backup reminder; empty (first use); loading; large text 200%.

**Reading and focus order.** Title (heading), Settings, search field, reminder banner (when shown), day headings and entries, New entry.

**Accessible names and announcements.** Each entry row reads its time, then its preview text. Search field: "Search your entries". Reminder actions: "Export now" and "Later".

**Edge cases.** Empty and loading are different states. The reminder never contains entry text. At 200% the header stacks and the New entry button grows with its text.

**Open questions.** none: reminder after more than 30 days, hidden for 7 days; journal size 20,000 entries.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | Entries on different dates; The timeline opens; Entries are listed newest first, grouped by day | FEAT-002 AC-1 | TC-016 |
| AC-2 | No entries; The timeline opens; An empty state with an action to write the first entry is shown | FEAT-002 AC-2 | TC-017 |
| AC-3 | An entry in the list; The user taps it; The entry opens | FEAT-002 AC-3 | TC-018 |
| AC-4 | A very large journal; The user scrolls quickly; Scrolling stays smooth: at least 95% of frames render within 16.7 ms (`nfr-targets-v1`, a journal of 20,000 entries, provisional until measured) | FEAT-002 AC-4 | TC-019 |
| AC-5 | The app is locked; The timeline would be shown; No entry text is visible | FEAT-002 AC-5 | TC-020 |
| AC-6 | No export was ever made and the journal has entries; The timeline opens; The reminder is shown | FEAT-008 AC-1 | TC-068 |
| AC-7 | A recent export and no new entries since; The timeline opens; No reminder is shown | FEAT-008 AC-2 | TC-069 |
| AC-8 | The last export is more than 30 days old and new entries exist; The timeline opens; The reminder is shown | FEAT-008 AC-3 | TC-070 |
| AC-9 | The reminder is shown; The user dismisses it; It is hidden for 7 days | FEAT-008 AC-4 | TC-071 |
| AC-10 | A successful export; It completes; The reminder disappears | FEAT-008 AC-5 | TC-072 |
| AC-11 | The reminder; It is shown; It contains no entry text | FEAT-008 AC-6 | TC-073 |
| AC-12 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-13 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-14 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S7 Editor

| Field | Value |
|-------|-------|
| Screen | S7 Editor |
| Feature | FEAT-001 |
| Status | draft |
| Design file | `design/features/journal/journal-light.pen` and `-dark.pen` |
| Flow context | F3 in `ux-design/report/user-flows.md` |

**Goal.** Write or change an entry safely. Primary action: Save.

**States drawn.** New; editing; long entry; delete confirmation; resume unsaved draft; save error; large text 200%.

**Reading and focus order.** Back, date, Save, draft status, entry text, Delete entry (existing entries only).

**Accessible names and announcements.** Entry text area: "Entry text". Date: "Entry date, Saturday 20 September 2026, change". Delete sheet and draft dialog are announced as dialogs.

**Edge cases.** The draft is kept as the user types, so leaving the screen never loses text. The save error keeps the text and offers Try again. The "Save" action next to "Draft kept safely" is tested for confusion in RS-001.

**Open questions.** none: no artificial maximum length; the open-failure state is S14.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The app is unlocked; The user writes text and saves; A new entry with that text and today's date appears in the timeline | FEAT-001 AC-1 | TC-001 |
| AC-2 | An existing entry; The user changes its text and saves; The entry shows the new text | FEAT-001 AC-2 | TC-002 |
| AC-3 | A saved entry; The app is closed and opened again; The saved text is unchanged | FEAT-001 AC-3 | TC-003 |
| AC-4 | The user is typing an unsaved entry; The app is force-closed; The unsaved text is offered back at the next unlock | FEAT-001 AC-4 | TC-004 |
| AC-5 | An existing entry; The user changes its date and saves; The entry appears under the chosen date in the timeline | FEAT-001 AC-5 | TC-005 |
| AC-6 | An existing entry; The user deletes it and confirms; The entry no longer appears in the timeline | FEAT-001 AC-6 | TC-006 |
| AC-7 | An existing entry; The user starts to delete and then cancels; The entry is unchanged | FEAT-001 AC-7 | TC-007 |
| AC-8 | An empty editor; The user tries to save; No entry is created | FEAT-001 AC-8 | TC-008 |
| AC-9 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-10 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-11 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S8 Search

| Field | Value |
|-------|-------|
| Screen | S8 Search |
| Feature | FEAT-005 |
| Status | draft |
| Design file | `design/features/journal/journal-light.pen` and `-dark.pen` |
| Flow context | F4 in `ux-design/report/user-flows.md` |

**Goal.** Find an old entry by a word. Primary action: type a word.

**States drawn.** Empty query; results; no results.

**Reading and focus order.** Back, search field, result count, results.

**Accessible names and announcements.** Search field: "Search your entries". Result rows name their date and matched word.

**Edge cases.** Query and results are kept when returning from an entry. The matched word is shown in a note because inline highlighting cannot be drawn in the design tool.

**Open questions.** none: matching rules decided.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | Entries that contain a word; The user searches for that word; The matching entries are listed with the word highlighted | FEAT-005 AC-1 | TC-040 |
| AC-2 | No entry contains the query; The user searches; A no-results message is shown | FEAT-005 AC-2 | TC-041 |
| AC-3 | A result in the list; The user taps it; The entry opens | FEAT-005 AC-3 | TC-042 |
| AC-4 | An entry that was deleted and contained the word; The user searches for it; The entry is not listed | FEAT-005 AC-4 | TC-043 |
| AC-5 | No network connection; The user searches; Search works normally | FEAT-005 AC-5 | TC-044 |
| AC-6 | A very large journal; The user searches; Results appear within 300 ms (`nfr-targets-v1`, provisional until measured) | FEAT-005 AC-6 | TC-045 |
| AC-7 | An entry that contains the word Café; The user searches for cafe or CAFÉ; The entry is listed | FEAT-005 AC-7 | TC-047 |
| AC-8 | An entry that contains the word river; The user searches for riv, and then for iver; The first search lists the entry and the second does not | FEAT-005 AC-8 | TC-095 |
| AC-9 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-10 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-11 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S9 Settings

| Field | Value |
|-------|-------|
| Screen | S9 Settings |
| Feature | FEAT-009 |
| Status | draft |
| Design file | `design/features/backup-and-settings/backup-and-settings-light.pen` and `-dark.pen` |
| Flow context | F5, F6 in `ux-design/report/user-flows.md` |

**Goal.** Change appearance and security, and reach export and import.

**States drawn.** Default; language choice; lock timeout choice.

**Reading and focus order.** Back, title, Appearance (Theme, Language), Security, Backup, About; each row reads its label then its value.

**Accessible names and announcements.** Switch: "Face or fingerprint, on". Rows with a chevron are buttons that open a screen or a choice sheet. Language choices are announced in their own language.

**Edge cases.** Row values wrap under their label at large text. The language default follows the phone, and English is used when the phone is neither English nor Indonesian. The lock timeout default is Immediately.

**Open questions.** none.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The settings screen; The user picks a theme; The app switches and keeps the choice after a restart | FEAT-009 AC-1 | TC-075 |
| AC-2 | A new lock timeout is chosen; The app stays in the background that long; The app locks | FEAT-009 AC-5 | TC-079 |
| AC-3 | Biometrics are turned off; The app is locked; Only the passcode unlocks it | FEAT-009 AC-6 | TC-080 |
| AC-4 | The settings screen; The user picks Indonesian; All app text is in Indonesian and stays so after a restart | FEAT-009 AC-8 | TC-098 |
| AC-5 | A phone set to a language other than English or Indonesian; The app is opened for the first time; The app is in English | FEAT-009 AC-9 | TC-099 |
| AC-6 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-7 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-8 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S10 Change passcode

| Field | Value |
|-------|-------|
| Screen | S10 Change passcode |
| Feature | FEAT-009 |
| Status | draft |
| Design file | `design/features/backup-and-settings/backup-and-settings-light.pen` and `-dark.pen` |
| Flow context | F1 in `ux-design/report/user-flows.md` |

**Goal.** Change the passcode without losing access. Primary action: Change passcode.

**States drawn.** Default; wrong current passcode; passcodes differ; done.

**Reading and focus order.** Back, title, Current passcode, New passcode, Repeat new passcode, Change passcode.

**Accessible names and announcements.** Same secure-field rules as S2.

**Edge cases.** A change that is interrupted must leave the journal openable (TC-082).

**Open questions.** none: same passcode rules as setup.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The user knows the current passcode; They change it; The new passcode opens the app | FEAT-009 AC-2 | TC-076 |
| AC-2 | The user knows the current passcode; They change it; All entries are still readable | FEAT-009 AC-3 | TC-077 |
| AC-3 | A wrong current passcode; The user tries to change it; The change is refused | FEAT-009 AC-4 | TC-078 |
| AC-4 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-5 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-6 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S11 Export

| Field | Value |
|-------|-------|
| Screen | S11 Export |
| Feature | FEAT-006 |
| Status | draft |
| Design file | `design/features/backup-and-settings/backup-and-settings-light.pen` and `-dark.pen` |
| Flow context | F5 in `ux-design/report/user-flows.md` |

**Goal.** Make a backup file. Primary action: Continue, then Export.

**States drawn.** Choose; set password; readable-by-anyone warning; working; done; error.

**Reading and focus order.** Back, title, options, Continue; then password fields, Export; then progress with Cancel.

**Accessible names and announcements.** Options are radio buttons named by their title and description. The progress bar reads its caption, updated at intervals, not continuously.

**Edge cases.** The password step says the export password is not the passcode and cannot be recovered. The warning names the consequence before the destructive-styled button. An error keeps the last export time unchanged.

**Open questions.** Vocabulary Export versus Back up is tested in RS-001.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | An encrypted export; The file is opened without the password; The entries cannot be read | FEAT-006 AC-1 | TC-048 |
| AC-2 | An encrypted export; A wrong password is used; No entries are revealed | FEAT-006 AC-2 | TC-049 |
| AC-3 | The plaintext option; The user selects it; A warning is shown before anything is created | FEAT-006 AC-3 | TC-050 |
| AC-4 | A finished export; Its entry count is read; The count equals the number of entries in the journal | FEAT-006 AC-4 | TC-051 |
| AC-5 | An entry that was deleted; An export is made; The deleted entry is not in the file | FEAT-006 AC-5 | TC-052 |
| AC-6 | A successful export; It completes; The last export time is updated | FEAT-006 AC-6 | TC-053 |
| AC-7 | A failed export; It ends with an error; The last export time is not changed | FEAT-006 AC-7 | TC-054 |
| AC-8 | A very large journal; An export runs; Progress is shown | FEAT-006 AC-8 | TC-055 |
| AC-9 | The export password step; The user enters a password of fewer than 8 characters; The password is refused with a reason and no file is created | FEAT-006 AC-9 | TC-096 |
| AC-10 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-11 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-12 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S12 Import

| Field | Value |
|-------|-------|
| Screen | S12 Import |
| Feature | FEAT-007 |
| Status | draft |
| Design file | `design/features/backup-and-settings/backup-and-settings-light.pen` and `-dark.pen` |
| Flow context | F6 in `ux-design/report/user-flows.md` |

**Goal.** Restore a journal from an export. Primary action: Choose a file, then Import.

**States drawn.** Choose file; password; wrong password; working; result; damaged file; newer version; wrong file.

**Reading and focus order.** Back, title, body, Choose a file; then password field, Import; then progress, Cancel; then result.

**Accessible names and announcements.** Errors are announced when they appear and always say that nothing was changed.

**Edge cases.** Every error path ends in a defined state and leaves the journal unchanged. The counts in the result are illustrative.

**Open questions.** none: merge rule and restore order decided.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | An export of N entries and a fresh install; It is imported; All N entries are present with the same text and dates | FEAT-007 AC-1 | TC-058 |
| AC-2 | An encrypted file; A wrong password is entered; The import is refused and nothing changes | FEAT-007 AC-2 | TC-059 |
| AC-3 | A damaged or cut-off file; It is imported; The import is refused and the journal is unchanged | FEAT-007 AC-3 | TC-060 |
| AC-4 | A file made by an older app version; It is imported; All its entries are restored | FEAT-007 AC-4 | TC-061 |
| AC-5 | A file made by a newer unsupported version; It is imported; A message asks the user to update the app and nothing changes | FEAT-007 AC-5 | TC-062 |
| AC-6 | A journal that already has some of the file's entries; The file is imported; No duplicates are created | FEAT-007 AC-6 | TC-063 |
| AC-7 | An import that is interrupted midway; The app is opened again; The journal is unchanged | FEAT-007 AC-7 | TC-064 |
| AC-8 | An entry with the same id exists on the phone with different text; The file is imported; The entry already on the phone is kept unchanged and counted as skipped | FEAT-007 AC-8 | TC-097 |
| AC-9 | A fresh install where the user chose to import a backup; The import starts; A passcode has already been set | FEAT-007 AC-9 | TC-102 |
| AC-10 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-11 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-12 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S13 About and privacy

| Field | Value |
|-------|-------|
| Screen | S13 About and privacy |
| Feature | FEAT-009 |
| Status | draft |
| Design file | `design/features/backup-and-settings/backup-and-settings-light.pen` and `-dark.pen` |
| Flow context | none in `ux-design/report/user-flows.md` |

**Goal.** State plainly what the app does and does not do with data.

**States drawn.** Default.

**Reading and focus order.** Back, title (heading), four points (list).

**Accessible names and announcements.** The points form a list.

**Edge cases.** Every claim must trace to a requirement and a test (TC-081); a claim with no control behind it is removed.

**Open questions.** Indonesian text of the statement is reviewed by the owner.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The privacy statement; It is reviewed; Every claim traces to a requirement and a test | FEAT-009 AC-7 | TC-081 |
| AC-2 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-3 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-4 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |

## S14 Journal cannot be opened

| Field | Value |
|-------|-------|
| Screen | S14 Journal cannot be opened |
| Feature | FEAT-001 |
| Status | draft |
| Design file | `design/features/setup-and-lock/setup-and-lock-light.pen` and `-dark.pen` |
| Flow context | none (new) in `ux-design/report/user-flows.md` |

**Goal.** Recover when the journal cannot be opened (for example damaged data). Primary action: Try again.

**States drawn.** Error.

**Reading and focus order.** Title, body, Try again, Import your journal.

**Accessible names and announcements.** The message is announced on arrival.

**Edge cases.** Nothing has been deleted, and the screen says so. It is also shown after a failed data migration once the journal has been restored (ADR-006). The behaviour was decided on 2026-09-20 and is FEAT-001 AC-9.

**Open questions.** none.

| AC | Criterion | Source | Test |
|----|-----------|--------|------|
| AC-1 | The journal cannot be opened (damaged data, or a key that does not match); The app starts and unlocks; A message says nothing was deleted and offers Try again and Import your journal | FEAT-001 AC-9 | TC-092 |
| AC-2 | A journal last saved by a newer version of the app; An older version of the app opens it; The older version refuses to change it and asks the user to update the app | FEAT-001 AC-10 | TC-101 |
| AC-3 | An update whose data migration fails; The app starts; The journal is restored to its state before the update and opens | FEAT-001 AC-11 | TC-100 |
| AC-4 | Every control has a visible label or visible text, and an accessible name | design | TC-087 |
| AC-5 | Text stays readable at 200 percent system font size in both themes | design | TC-088 |
| AC-6 | Text contrast and touch targets meet the tokens in both themes | design | TC-089 |
